# Tickets moved

Tickets are now managed inside the Obsidian vault at:

- `hubo_kb/tickets/`
- `hubo_kb/tickets/pending/`
- `hubo_kb/tickets/done/`

This folder is kept only as a compatibility pointer.
